fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-dotenv==1.0.1
sentence-transformers==2.2.2
# Pin huggingface-hub to prevent ImportError: cannot import name 'cached_download'
huggingface-hub<0.25.0
torch==2.2.1
langchain==0.2.12
langgraph==0.2.19
langchain-google-genai==1.0.6
chromadb==0.5.5
joblib==1.4.2
# Upgrade sklearn to match local training environment (1.7.2 seems unusually high, might be a dev version or 1.5.2 meant?)
# Wait, pypi latest is 1.5.2. 1.7.2 doesn't exist on PyPI stable. 
# If user has 1.7.2 locally, it might be from conda-forge or a specific source?
# Let's try to unpin it to get the latest available, or use >=1.5.
scikit-learn>=1.5
xgboost==2.1.1
pandas==2.2.3
numpy<2
redis==5.1.0
boto3==1.35.51
matplotlib==3.9.2
seaborn==0.13.2
